# Numercial-Analysis
这是一个用于提交数值分析大作业的程序，支持输入非线性函数进行可视化，并使用不同的迭代函数求根
打开方法如下：

1.打开终端，确保已经安装python

2.安装python库

pip install matplotlib numpy sympy

3.在终端中运行python fanc.py

4.在打开的页面中输入各项数值进行实验

注意：打开的页面内有默认函数，发现框里有值是正常现象
